{
  "name": "user-api",
  "projectType": "application",
  "sourceRoot": "apps/user-api/src",
  "targets": {
    "lint": {
      "executor": "nx:run-commands",
      "options": {
        "command": "npm run lint",
        "cwd": "apps/user-api"
      }
    },

    "test": {
      "executor": "nx:run-commands",
      "outputs": ["coverage/apps/user-api"],
      "options": {
        "command": "npm test -- --coverage",
        "cwd": "apps/user-api"
      }
    },

    "build": {
      "executor": "nx:run-commands",
      "outputs": ["dist/apps/user-api"],
      "options": {
        "command": "npm run build",
        "cwd": "apps/user-api"
      }
    },

    "docker-build": {
      "executor": "nx:run-commands",
      "dependsOn": ["build"],
      "options": {
        "command": "docker build -t alqoseemi/user-api:${TAG:-latest} -f Dockerfile .",
        "cwd": "apps/user-api"
      }
    },

    "docker-push": {
      "executor": "nx:run-commands",
      "dependsOn": ["docker-build"],
      "options": {
        "command": "docker push alqoseemi/user-api:${TAG:-latest}"
      }
    }
  },
  "tags": []
}
